# LTI – Talent Tracking System

## Agent Specs

Before working on any part of this codebase, read the relevant spec from `agent-specs/`:

| Scope | Spec file |
|---|---|
| Any file inside `backend/` | `agent-specs/backend.md` |

These files define the layer structure, code patterns, security requirements, and test conventions for this project. Apply them without being asked.

## Project context

See `openspec/config.yaml` for the full tech stack, directory layout, API conventions, and running instructions.
